{% extends 'web/base.html' %}

{% block contenido %}
<<<<<<< HEAD

<a  name="contenido"></a>
<div class="content-section-a">
    <div class="container">

      <form method="POST" class="post-form">{% csrf_token %}
          {% if exist %}
          <p style='color:#ff0000'>El correo <strong>{{ exist }}</strong> existe. Por favor introduce otro correo.</p>
          <button type="submit" class="save btn btn-default">Go back</button>
          {% elif created %}
          <p>Se ha enviado un correo electrónico a <strong>{{created}}</strong>. Por favor, sigue las instrucciones en él.</p>
          <button type="submit" class="save btn btn-default">Go back</button>
          {% else %}
          <h1>Por favor, completa el siguiente formulario</h1>
          {{ form.as_p }}
          <button type="submit" class="save btn btn-default">Save</button>
          {% endif %}
      </form>

    </div>
    <!-- /.contenido -->
</div>

=======
 
  <form method="POST" class="post-form">{% csrf_token %}
  <!-- esto es para probar otro metodo de enviar mensajes, lo mas probable es que lo borremos-->
  <!--
      {% if messages %}
      <ul class="messages">
      {% for message in messages %}
      <li{% if message.tags %} class="{{ message.tags }}"{% endif %}>{{ message }}</li>
      {% endfor %}
      </ul>
      {% endif %}
-->
      {% if exist %}
      <p style='color:#ff0000'>El correo <strong>{{ exist }}</strong> existe. Por favor introduce otro correo.</p>
      <button type="submit" class="save btn btn-default">Go back</button>
      {% elif created %}
      <p>Se ha enviado un correo electrónico a <strong>{{created}}</strong>. Por favor, sigue las instrucciones en él.</p>
      <button type="submit" class="save btn btn-default">Go back</button>
      {% else %}
      <h1>Por favor, completa el siguiente formulario</h1>
      {{ form.as_p }}
      <button type="submit" class="save btn btn-default">Save</button>
      {% endif %}
  </form>
>>>>>>> ef2e9e3eebba157a52fa236a5b5244a41a3a048b
{% endblock %}
